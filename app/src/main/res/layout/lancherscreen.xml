<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimaryDark"
    android:fitsSystemWindows="true"
    tools:context=".controller.LauncherScreen">

    <!-- The primary full-screen view. This can be replaced with whatever view
         is needed to present your content, e.g. VideoView, SurfaceView,
         TextureView, etc. -->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center_horizontal"
        android:orientation="vertical">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/pillbox_icon"
            android:layout_width="@dimen/big_img_width_height"
            android:layout_height="@dimen/img_width_height"
            android:src="@drawable/pillbox" />

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/pillbox_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textColor="@color/intro_font"
            android:textSize="60sp"
            android:textStyle="bold"
            android:gravity="center"
            android:shadowColor="#3f3f3f"
            android:shadowDx="-2"
            android:shadowDy="-2"
            android:shadowRadius="1"
            android:fontFamily="@font/pacifico_regular"/>

    </LinearLayout>
</RelativeLayout>